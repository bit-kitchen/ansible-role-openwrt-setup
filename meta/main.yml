galaxy_info:
  author: bit_kitchen
  role_name: openwrt_setup

  description: Setup OpenWrt. Python NOT requried on OpenWrt!
  license: MIT

  min_ansible_version: 2.4

  # https://galaxy.ansible.com/api/v1/platforms/
  platforms:
  - name: GenericLinux
    versions:
    - any

  galaxy_tags:
  - openwrt

dependencies:
  - role: gekmihesg.openwrt
    vars:
      openwrt_install_recommended_packages: no
